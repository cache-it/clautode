---
description: Merge this repo's structure and principles into the local Claude install (~/.claude)
---

The user wants to **align the local Claude install to this repo**. Execute the
**Alignment protocol (repo → local)** defined in [`CLAUDE.md`](../../CLAUDE.md) — that
file is the single source for the protocol's steps and target paths; do not restate
them here, follow them there.

1. First **read `CLAUDE.md` and `GUIDELINES.md`** to acquire the project's knowledge.
2. Run the Alignment protocol exactly as written there, honouring its safety rails:
   **merge, don't clobber**; **confirm before any destructive step**; **keep local-only
   items** (never delete what the repo didn't create).
3. **Start by presenting the inventory and your proposed plan**, then proceed on
   confirmation, and **report** what was installed, merged, and skipped.
